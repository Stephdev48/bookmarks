{% extends 'base.html.twig' %}

{% block title %}Domain{% endblock %}

{% block body %}
    <h1>Domain</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ domain.id }}</td>
            </tr>
            <tr>
                <th>DomName</th>
                <td>{{ domain.domName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_domain_index') }}">back to list</a>

    <a href="{{ path('app_admin_domain_edit', {'id': domain.id}) }}">edit</a>

    {{ include('admin_domain/_delete_form.html.twig') }}
{% endblock %}
